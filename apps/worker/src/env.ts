/**
 * Worker bindings. Replace this alias with an
 * `interface Env { ... }` declaration once the
 * dispatcher acquires its first binding (D1, KV, R2,
 * queues, or service bindings).
 */
export type Env = Record<string, never>;
